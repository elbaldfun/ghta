# Go backend base URL. Server-side only (no NEXT_PUBLIC_ prefix) so the backend
# address stays out of the browser bundle.
API_URL=http://localhost:3000

# Public site origin, used for canonical URLs / sitemap / hreflang.
# Production: https://starrank.dev
NEXT_PUBLIC_SITE_URL=http://localhost:3001
